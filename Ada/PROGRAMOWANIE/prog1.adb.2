WITH Ada.Text_IO, Ada.Integer_Text_IO;
USE Ada.Text_IO, Ada.Integer_Text_IO;

PROCEDURE Prog1 IS
   A,
   B,
   C,
   G : Integer;
BEGIN
   Put("podaj G: ");
   Get(G);
   LOOP
      Put("A: ");
      Get(A);
      Put("B ");
      Get(B);
      Put("C: ");
      Get(C);

if (a>b and a>c and b>c) or (b>a and b>c and a>c) then

exit when

   END Prog1;
