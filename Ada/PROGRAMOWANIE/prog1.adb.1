WITH Ada.Text_IO, Ada.Integer_Text_IO;
USE Ada.Text_IO, Ada.Integer_Text_IO;

PROCEDURE Prog1 IS
   A,
   B,
   C,
   G,
   Suma,
   Ile  : Integer;
BEGIN
   Put("podaj G: ");
   Get(G);

   C:=0;
   B:=0;
   LOOP
      Put("Podaj liczbe: ");
      Get(A);
      EXIT WHEN A=0;
      IF C /= 0 AND B /= 0 THEN
         Put(C, 0);
         Put(", ");
         Put(B, 0);
         Put(", ");
         Put(A, 0);
      END IF;
      C:=B;
      B:=A;

   END LOOP;
END Prog1;
